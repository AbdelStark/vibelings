Make sure your output is valid JSON. Start with `{` and end with `}`.

---

Check that all required fields are present: `name`, `age`, and `email`. The `occupation` field is optional but recommended.

---

The `age` field must be an integer, not a string. Write `"age": 28`, not `"age": "28"`.

---

The email must match the pattern: letters, numbers, dots before @, then domain. Example: `alex.chen@techcorp.io`
